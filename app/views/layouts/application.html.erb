<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Domi: College Housing Made Easy" %></title>

    <%= stylesheet_link_tag    "application" %>
    <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.8.2/jquery.js"></script>
    <%= javascript_include_tag "vendor/modernizr", "nested_form", "application" %>
    <%= csrf_meta_tags %>
    <%= favicon_link_tag 'domiapp.png' %>
  </head>

  <body>
    <% if signed_in? %>
      <%= render "layouts/navigation" %>
      <%= yield %>
    <% else %>
      <%= yield %>
    <% end %>
  </body>
</html>